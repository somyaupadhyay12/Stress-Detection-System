# LaTeX report

`main.tex` is the standalone research-style report. Its selected PNG figures are in `figures/`; the complete visual archive remains in `../plots/`.

Compile locally after installing a standard TeX distribution:

```powershell
pdflatex main.tex
pdflatex main.tex
```
